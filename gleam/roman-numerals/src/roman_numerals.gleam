pub fn convert(number: Int) -> String {
  let quotient = number / 10
  case quotient {
    _ if quotient < 1 -> {
      todo
      "base case"
    }
    _ -> {
      todo
      "recurse on the remainder"
    }
  }
  // case number {
  //   _ if number / 1000 >= 1 {

  //   }
  // }
}
